# 自选维度分析 — 完整设计方案

## 一、项目背景

为 `ye-annotation-site`（叶德辉书信 NER 标注检索平台）新增一个独立的"自选维度分析"页面，让学者和访客能自由组合多个标注维度，进行交叉统计分析和可视化探索。

### 当前数据概况

| 指标 | 数值 |
|------|------|
| 书信总数 | **306** 封 |
| 年份范围 | 1894–1926（**28 个年份**），**76 封无年份**（占 25%） |
| 收信人 | **58** 位（含群体收件人） |
| 规范实体 | **3,378** 个 |
| 实体类型 | 9 类：PER(516) / LOC(90) / BOK(1,095) / VER(342) / TIM(771) / OFF(116) / ORG(135) / KIN(153) / AST(160) |
| 事件类型 | 5 类：BIB(文献) / ACA(学术) / SOC(交往) / POL(时局) / FAM(家族) |
| 行为类型 | 7 类：REQ / DSP / INF / PRS / MNT / INS / NEG |
| 数据源 | `data/generated_with_acts.json`（约 8MB） |
| 计算模式 | 纯客户端，静态 JSON 已通过 `lib/data-adapter.ts` 加载 |

---

## 二、页面定位

- **路由**：`/analysis`
- **导航**：主导航新增"维度分析"入口
- **用户群**：学者（深度探索数据）+ 访客（快速获取直观认知）
- **风格**：与现有页面保持一致（中文为主，学术气质）

---

## 三、页面布局

### 大屏（≥1280px）：三栏布局

```
┌──────────────────────────────────────────────────────────────┐
│  📊 自选维度分析                    [预设模板▼] [导出CSV] [重置] │
├──────────┬────────────────────┬──────────────────────────────┤
│          │                    │                              │
│ 行维度    │   📋 数据透视表      │   📈 图表                     │
│ [实体类型] │                    │                              │
│  ▸ 子类型  │  ┌────┬────┬────┐ │   ┌────────────────────┐     │
│          │  │    │1894│1895│ │   │ ████████            │     │
│ 列维度    │  │PER │ 12 │ 15 │ │   │ ████████████        │     │
│ [年份]    │  │ LOC│  8 │ 10 │ │   │ ██████              │     │
│          │  │BOK │  5 │  7 │ │   │ 堆叠柱状图/热力图/…   │     │
│ 值       │  │ …  │    │    │ │   └────────────────────┘     │
│ [书信数]  │  └────┴────┴────┘ │                              │
│          │                    │  [柱状图▼] [折线图▼] [热力图▼] │
│ 图表类型  │  [复制表格]         │  [桑基图▼] [雷达图▼]          │
│ [柱状图]  │                    │                              │
│          │  共 N 行 × M 列     │                              │
│ ──────── │                    │                              │
│ 筛选     │                    │                              │
│ 收信人 = │                    │                              │
│  松崎鹤雄 │                    │                              │
│ + 添加   │                    │                              │
│          │                    │                              │
│ ⬜ 排除   │                    │                              │
│  无年份   │                    │                              │
└──────────┴────────────────────┴──────────────────────────────┘
```

- **左侧维度配置面板**：约 280px 宽
- **中间透视表**：弹性宽度，可横向滚动
- **右侧图表**：弹性宽度，与透视表同步更新

### 平板（768px-1279px）：两栏

- 左侧面板 + 上方透视表/下方图表

### 手机（<768px）：单栏标签页

- 维度配置 / 透视表 / 图表 三个标签页切换

---

## 四、维度体系

### 4.1 可用维度一览

| 类别 | 维度 ID | 中文名 | 数据来源 | 值数量 | 支持嵌套 |
|------|---------|--------|---------|--------|---------|
| 时间 | `year` | 年份 | `letter.year` | 28 + "未知" | — |
| 时间 | `period` | 时期 | 推导：晚清(1894-1911) / 民初(1912-1926) | 2 | — |
| 人物 | `recipient` | 收信人 | `letter.recipient` | 58 | — |
| 实体 | `entity_type` | 实体类型 | `entityStats` keys | 9 | ▸ `entity_subtype` |
| 实体 | `entity_subtype` | 实体子类型 | `entityCatalog[].subtypes` | ~50 | — |
| 实体 | `entity_canonical` | 具体实体 | `entityCatalog[].canonical` | 3,378 | — |
| 空间 | `loc_subtype` | 地点子类型 | LOC 的 subtypes | 9 | — |
| 空间 | `loc_canonical` | 具体地点 | LOC 规范实体 | 90 | — |
| 事件 | `event_type` | 事件类型 | `eventStats` keys | 5 | — |
| 行为 | `act_type` | 行为类型 | `actStats` keys | 7 | — |
| 内容 | `bok_subtype` | 书籍子类型 | BOK 的 subtypes | 7 | — |
| 内容 | `ver_subtype` | 版本子类型 | VER 的 subtypes | 11 | — |

### 4.2 度量指标（值）

| 指标 ID | 中文名 | 含义 |
|---------|--------|------|
| `letter_count` | 书信数 | 去重计数（一封书信只算一次） |
| `mention_count` | 实体出现次数 | 实体 mention 总数 |
| `canonical_count` | 规范实体数 | 不同规范实体的数量 |
| `event_count` | 事件数 | 事件 occurrence 总数 |
| `paragraph_count` | 行为段落数 | 行为标注段落总数 |

### 4.3 一封信可属于多个类别

例如一封 1913 年的信包含 PER 和 BOK 两种实体，在"行=实体类型 × 列=年份 × 值=书信数"交叉表中：

- (PER, 1913) = 1 封
- (BOK, 1913) = 1 封
- 所有格子之和 ≥ 实际总书信数

这是合理且常见的多维分析行为，页面上以 Tooltip 说明。

---

## 五、嵌套维度

支持两级嵌套。当用户选择某维度后，可展开其子维度：

```
行维度选择器：
┌──────────────────────────┐
│ 实体类型            [▾]  │
│   ☐ 展开子类型：实体子类型  │  ← 勾选后表格变为两级行头
└──────────────────────────┘
```

### 嵌套表格效果

```
┌──────────────────────────────┐
│                     1894 1895│
├──────────────────────────────┤
│ PER                           │
│   ├─ PER-SELF          1   2  │
│   ├─ PER-CONTEMPORARY  5   8  │
│   ├─ PER-HISTORICAL    0   1  │
│   └─ ……                      │
│ LOC                           │
│   ├─ ADM1              3   4  │
│   ├─ URB               2   3  │
│   └─ ……                      │
│ BOK                           │
│   ├─ BOK-CLASSICS      4   6  │
│   ├─ BOK-HISTORY       3   3  │
│   └─ ……                      │
└──────────────────────────────┘
```

行头和列头均可嵌套，但不建议超过两级（否则表格过于复杂）。

---

## 六、筛选系统

### 6.1 筛选类型

| 筛选类型 | 适用维度 | UI 组件 | 示例 |
|----------|---------|---------|------|
| 等于 / 不等于 | 分类维度 | 下拉搜索 | 收信人 = 松崎鹤雄 |
| 包含于 / 排除 | 分类维度 | 多选 Tag | 实体类型 ∈ {PER, BOK} |
| 范围 | 有序维度 | 双滑块 / 输入框 | 年份 ∈ [1911, 1920] |

### 6.2 多条筛选

多条筛选之间为 **AND** 逻辑：

```
┌──────────────────────────────────────┐
│ 筛选条件                              │
│                                      │
│ 收信人      = 松崎鹤雄          [×]   │
│ 实体类型    ⊂ {PER, BOK}       [×]   │
│ 年份        ∈ [1911, 1920]     [×]   │
│                                      │
│ + 添加筛选                            │
└──────────────────────────────────────┘
```

### 6.3 具体实体选择器

针对 3,378 个实体的选择体验：

- 🔍 **搜索框**：支持汉字、拼音首字母、别名搜索
- 🏷️ **类型筛选**：按实体类型折叠面板快速过滤
- 📊 **按出现次数降序**：最常用的实体排在前面
- ✅ **多选 + Tag 删除**：已选实体以 Tag 形式展示
- 💡 **热门推荐**：Top 20 高频实体快捷选择

---

## 七、未知年份处理

76 封无年份书信（占 25%），采用三种策略并存：

### 默认行为
- 年份维度中显示"未知（76封）"为一个独立类别
- 参与所有透视表和图表计算

### 排除开关
```
⬜ 排除无年份书信（当前 76 封）
```
- 勾选后，所有 `year === null` 的书信从计算中剔除
- 透视表和图表实时更新

### 提示
- 当选择年份作为维度时，面板顶部显示淡色提示：
  > ⚠️ 当前有 76 封书信无年份信息，可选择排除以聚焦确定性数据

---

## 八、图表系统（ECharts）

### 8.1 图表类型 × 分析场景

| 图表类型 | ECharts Series | 最佳场景 | 维度配置 |
|----------|---------------|---------|---------|
| **堆叠柱状图** | `bar` + `stack` | 二维交叉，分类适中 | 行(≤15类) × 列(≤20类) |
| **热力图** | `heatmap` | 二维交叉，分类多 | 行(任意) × 列(任意) |
| **折线图/面积图** | `line` + `areaStyle` | 时间趋势 | 列=年份 |
| **环形图** | `pie` + `radius: ['40%','70%']` | 一维分布 | 仅行维度，无列维度 |
| **玫瑰图** | `pie` + `roseType: 'area'` | 一维分布（强调差异） | 仅行维度，无列维度 |
| **桑基图** | `sankey` | 维度流向关系 | 行=分类A, 列=分类B |
| **雷达图** | `radar` | 多维度对比 | 单个筛选主体 × 多维指标 |
| **气泡图** | `scatter` + `symbolSize` | 三维关系 | 行(>30个) × 列 × 值大小 |

### 8.2 ECharts 优势

- 原生支持以上全部图表类型
- 完美中文排版（tooltip、legend、axis 标签）
- 丰富交互：缩放、hover 高亮、点击下钻、图例切换
- 自适应容器大小（`resize` 监听）
- 内置图片导出（PNG/SVG）
- 体积：按需引入约 300-500KB（gzip 后约 100-150KB）

### 8.3 图表交互行为

- **Hover**：tooltip 显示交叉格的具体数值和占比
- **点击柱/格**：高亮并联动透视表滚动到对应行
- **图例点击**：切换显示/隐藏某类别
- **缩放**：柱状图/折线图支持横向缩放（`dataZoom`）
- **导出**：右键或工具栏按钮导出为图片

---

## 九、预设分析模板

帮助访客快速上手，一键载入预设配置：

| # | 模板名 | 行维度 | 列维度 | 值 | 默认图表 |
|---|--------|--------|--------|-----|---------|
| 1 | 📅 书信年代分布 | 收信人 | 年份 | 书信数 | 堆叠柱状图 |
| 2 | 🏷️ 实体类型年代趋势 | 实体类型 | 年份 | 实体出现次数 | 热力图 |
| 3 | 📚 书籍与事件关联 | 书籍子类型 | 事件类型 | 书信数 | 桑基图 |
| 4 | 👤 收信人关注画像 | 收信人 | 事件类型 | 事件数 | 热力图 |
| 5 | 🗺️ 地点提及变迁 | 地点子类型 | 年份 | 提及次数 | 折线图 |
| 6 | 📖 版本讨论轨迹 | 版本子类型 | 年份 | 书信数 | 面积图 |

模板以按钮或卡片形式展示在页面顶部，点击后自动填充所有配置并渲染结果。

---

## 十、导出功能

### 10.1 导出 CSV
- 将当前透视表数据导出为 UTF-8 BOM CSV 文件
- Excel 直接双击打开不乱码
- 保留行头、列头、合计行列

### 10.2 复制表格
- 一键复制为 TSV 格式到剪贴板
- 可粘贴到 Excel / Google Sheets / 飞书表格

### 10.3 导出图表
- ECharts 原生支持，工具栏按钮导出 PNG / SVG

---

## 十一、技术实现要点

### 11.1 组件树（建议）

```
app/analysis/page.tsx
├── AnalysisPage (客户端组件)
│   ├── AnalysisHeader          — 标题、模板选择器、导出/重置按钮
│   ├── DimensionPanel          — 左侧维度配置（行/列/值/筛选）
│   │   ├── DimensionSelector   — 维度下拉 + 嵌套勾选
│   │   ├── MetricSelector      — 度量指标下拉
│   │   ├── FilterList          — 筛选条件列表
│   │   │   └── FilterRow       — 单条筛选（维度 + 运算符 + 值）
│   │   └── EntitySearchModal   — 具体实体搜索弹窗
│   ├── PivotTable              — 中间透视表
│   │   ├── PivotHeader         — 列头（支持嵌套）
│   │   ├── PivotBody           — 数据行（支持嵌套行头）
│   │   └── PivotFooter         — 合计行 / 行数统计
│   └── ChartPanel              — 右侧图表区
│       └── EChartsView         — ECharts 实例封装
│
├── lib/analysis/
│   ├── types.ts                — 分析配置/结果类型定义
│   ├── dimensions.ts           — 维度元数据定义
│   ├── aggregator.ts           — 交叉聚合计算引擎
│   ├── echarts-builder.ts      — ECharts option 构建器
│   └── templates.ts            — 预设分析模板
│
└── components/analysis/         — 拆分后的分析组件
```

### 11.2 核心计算引擎 (`aggregator.ts`)

```typescript
// 伪代码示意
interface AnalysisConfig {
  rowDimension: DimensionId;
  rowNested?: DimensionId;       // 可选嵌套
  columnDimension: DimensionId;
  columnNested?: DimensionId;
  metric: MetricId;
  filters: Filter[];
  excludeUnknownYear: boolean;
}

interface PivotResult {
  rowHeaders: string[][];        // [主行头, 子行头]
  columnHeaders: string[][];     // [主列头, 子列头]
  cells: number[][];             // 交叉值矩阵
  rowTotals: number[];
  columnTotals: number[];
  grandTotal: number;
}

function computePivot(config: AnalysisConfig, dataset: Dataset): PivotResult {
  // 1. 遍历 dataset.letters，应用 filters
  // 2. 按配置的维度分组，根据 metric 类型计算值
  // 3. 构建行头/列头/值矩阵
  // 4. 计算合计
}
```

### 11.3 技术要点

- **纯客户端计算**：计算完全在浏览器中完成，无需 API 调用
- **性能优化**：306 封信 × 3,378 个实体，全维度交叉计算复杂度可控（< 100ms）
- **Web Worker（可选）**：如果后续数据量增长导致 UI 卡顿，可将 `aggregator.ts` 放入 Worker
- **URL 参数同步**：分析状态编码到 URL query params（`useSearchParams`），便于分享分析视图
- **响应式**：使用 CSS Grid + `useMediaQuery` 实现三栏/两栏/标签页自适应

---

## 十二、二期规划：AI 智能分析

### 12.1 定位

AI 是锦上添花的增强功能，不是 MVP 必须。建议第一期先把纯前端的分析核心跑通，让学者手动选维度分析。第二期加入 AI 降低使用门槛。

### 12.2 AI 功能：自然语言 → 自动配置维度

用户在一个输入框中用自然语言描述问题，AI 自动配置好所有维度：

```
┌────────────────────────────────────────────────────────┐
│ 🤖 用自然语言描述你想分析的问题……                         │
│                                                        │
│  "帮我看看松崎鹤雄在1913到1917年间主要讨论哪些类型的书"     │
│                                                        │
│                                              [🔍 分析] │
└────────────────────────────────────────────────────────┘
```

**技术方案**：

- 前端将当前可用维度元数据（仅维度名、值列表大小，不传实际数据）+ 用户输入发送到 API
- API（复用 `ye_project` 的 DeepSeek key）返回结构化的维度配置 JSON
- 前端接收后自动填入维度面板，触发计算和渲染
- 一次调用成本约 ¥0.001–0.005（DeepSeek 价格极低）

**API 返回格式**：

```json
{
  "rowDimension": "bok_subtype",
  "columnDimension": "year",
  "metric": "letter_count",
  "filters": [
    { "dimension": "recipient", "operator": "equals", "value": "松崎鹤雄" },
    { "dimension": "year", "operator": "between", "value": ["1913", "1917"] }
  ],
  "chartType": "stacked_bar",
  "explanation": "已为你配置：行=书籍子类型，列=年份，筛选收信人为松崎鹤雄且年份在1913-1917之间"
}
```

### 12.3 AI 功能：自动洞察摘要（可选）

分析结果渲染后，一个"✨ AI 解读"按钮，点击后生成 2-3 句数据洞察：

> "1914 年是松崎鹤雄通信中书籍讨论的高峰（12 封），集中在经部和史部。1917 年后版本类(VER)讨论明显增多，这可能与叶德辉当时的藏书整理活动有关。"

- 传给 AI 的是**聚合后的统计摘要**（几 KB），不需要原始数据
- 标注"AI 生成，仅供参考"
- 对于学者用户，这是辅助参考而非替代判断

### 12.4 实现路径

```
第一期 MVP：纯前端分析页面（维度选择 + 透视表 + ECharts）
       ↓
    上线试用，收集反馈
       ↓
第二期增强：
  ├── NL → 维度配置（高优先级，大幅降低使用门槛）
  ├── AI 洞察摘要（中优先级，锦上添花）
  └── 分析视图分享链接（URL 参数同步）
```

---

## 十三、页面路由与导航

### 新增页面

```
/analysis          → 自选维度分析主页
```

### 导航修改

在 `SiteHeader` 主导航中新增"维度分析"入口，位于"实体分类检索"（/topics）之后。

### 导航结构（建议）

```
首页 (/)
├── 书信浏览 (/letters)
│   └── 书信详情 (/letter/[id])
├── 实体分类检索 (/topics)
│   ├── 人物 (/topics/people)
│   ├── 地点 (/topics/places)
│   ├── …… (其他专题)
│   ├── 分类详情 (/category/[layer]/[code])
│   └── 实体详情 (/entity/[type]/[name])
├── 维度分析 (/analysis)          ← 新增
└── 关于 (/about)
```

---

## 十四、待确认事项

| # | 事项 | 当前决定 | 是否需要调整 |
|---|------|---------|------------|
| 1 | 分析页面目标用户 | 学者 + 访客 | ✅ |
| 2 | 核心功能形态 | 透视表 + 图表组合 | ✅ |
| 3 | 图表库 | ECharts | ✅ |
| 4 | 维度嵌套 | 支持两级 | ✅ |
| 5 | 具体实体作为维度 | 开放，带搜索选择器 | ✅ |
| 6 | 未知年份处理 | 保留 + 排除开关 | ✅ |
| 7 | 度量值计算方式 | 多类别计数（一封可属多类） | ✅ |
| 8 | 收信人格式 | 保持原样（含群体收件人） | ✅ |
| 9 | 导出功能 | CSV + 复制 + 图表导出 | ✅ |
| 10 | AI 集成时机 | 第二期 | ✅ |
| 11 | 预设模板 | 6 个模板快速上手 | ✅ |

---

## 十五、文件清单

### 需要新建的文件

```
app/analysis/page.tsx               — 分析页面入口
lib/analysis/types.ts               — 分析配置/结果类型
lib/analysis/dimensions.ts          — 维度元数据定义
lib/analysis/aggregator.ts          — 交叉聚合计算引擎
lib/analysis/echarts-builder.ts     — ECharts option 构建器
lib/analysis/templates.ts           — 预设模板定义
components/analysis/AnalysisPage.tsx    — 分析页面主组件
components/analysis/DimensionPanel.tsx  — 维度配置面板
components/analysis/PivotTable.tsx      — 透视表组件
components/analysis/ChartPanel.tsx      — 图表面板组件
components/analysis/FilterRow.tsx       — 筛选条件行
components/analysis/EntitySearch.tsx    — 实体搜索选择器
components/analysis/TemplatePicker.tsx  — 模板选择器
```

### 需要修改的现有文件

```
components/SiteHeader.tsx            — 导航新增"维度分析"入口
package.json                         — 新增 echarts 依赖
```

---

> **文档版本**：v1.0  
> **创建日期**：2026-07-26  
> **状态**：设计阶段，待开发
