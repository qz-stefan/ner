# 自选维度分析 V3 — Codex 完整实现规范

> 本文档是给 Codex 的实现规范。**请完整阅读后再动手。**  
> V3 在 V2 基础上加入了详细的 UI/UX 设计要求，精简后台配置感，强化数字人文气质。

---

## 一、项目环境

| 项目 | 值 |
|------|-----|
| 项目路径 | `ye-annotation-site/` |
| 框架 | Next.js 16.2.6 (App Router) |
| UI | React 19.2.6 + Tailwind CSS 4.2.1 |
| 语言 | TypeScript 5.9.3 |
| 包管理 | npm |
| 数据 | 静态 JSON（约 8MB），通过 `@/lib/data-adapter` 导入 `dataset`，纯客户端计算 |
| 图表库 | ECharts 5 + echarts-for-react（**导入路径 `echarts-for-react/esm/core`**） |
| 构建工具 | Vite（通过 vinext） |

### 项目 CSS 变量（`app/globals.css`）

```css
:root {
  --ink: #27242a;
  --muted: #777178;
  --paper: #f8f6f0;
  --paper-deep: #f0ece2;
  --surface: #fffef9;
  --purple: #4f477e;
  --purple-deep: #35305d;
  --purple-pale: #efedf5;
  --line: #d9d4ca;
  --line-dark: #c7c0b5;
  --red: #944b48;
  --gold: #9a7c45;
  --green: #4f7367;
  --blue: #4f6782;
  --font-serif: "Noto Serif SC", "Songti SC", STSong, SimSun, serif;
  --font-sans: "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif;
}
```

全局 body 默认字体：`font-family: var(--font-sans)`（无衬线）。  
页面标题和正文内容应使用 `var(--font-serif)`（宋体/衬线）。

---

## 二、数据模型

数据来自 `import { dataset } from "@/lib/data-adapter"`。

```typescript
type EntityType = "PER" | "LOC" | "BOK" | "VER" | "TIM" | "OFF" | "ORG" | "KIN" | "AST";
type EventType = "BIB" | "ACA" | "SOC" | "POL" | "FAM";
type ActType = "REQ" | "DSP" | "INF" | "PRS" | "MNT" | "INS" | "NEG";

interface Letter {
  id: string;          // "001_1923_易培基"
  year: string | null; // "1894"–"1926" 或 null（76封无年份）
  recipient: string;   // 58 个不同收信人
  text: string;
}
```

### 关键配置

- `entityTypeMeta` — `{ PER: { label: "人物" }, LOC: { label: "地点" }, … }`（来自 `lib/config.ts`）
- `eventTypeMeta` — `{ BIB: { label: "文献活动" }, … }`
- `actTypeMeta` — `{ REQ: { label: "请求" }, … }`
- `getSecondaryCategories(entityCode)` — 返回二级分类列表，如 `[{ code: "PER-CONTEMPORARY", label: "同时代人" }, …]`（来自 `lib/topic-config.ts`）
- `annotationStyles` — 所有颜色定义

---

## 三、字体系统统一（⚠️ 关键）

### 3.1 问题诊断

当前分析页面存在严重的字体不一致问题：

- 页面主标题和模块标题使用了 `font-[var(--font-serif)]`（宋体）
- 但复选框标签、选项文字、按钮文字、标签文字、下拉菜单文字等**都继承 body 的 `font-family: var(--font-sans)`**（无衬线，Noto Sans SC / PingFang SC）
- 而项目的"优秀标注示例"页面（首页 `AnnotationPage` → `FeaturedLetterViewer`）中，所有书信正文、标题、标注说明均使用 `var(--font-serif)`
- 这导致分析页面看起来像后台配置表单（无衬线），而不是数字人文研究页面（宋体/衬线）

### 3.2 修复方案

**不能**只修主标题。分析页面中以下所有文字区域必须显式设置 `font-[var(--font-serif)]`：

- 页面主标题（"自选维度分析"）
- 页面副标题
- 模块标题（"选择分析维度""细分范围""事件与行为""分析结果"等）
- 分类标题（"时间""实体类型"等分组标签）
- **复选框标签文字**（所有维度选项文字）← 当前缺失
- **choice chip / 标签文字**（实体大类标签、二级分类标签）← 当前缺失
- **按钮文字**（"开始分析""预设模板""重置""导出CSV""复制表格"等）← 当前部分缺失
- **下拉菜单文字** ← 当前缺失
- 提示文字、空状态文字
- 分析结果区域文字

**唯一例外**：`PER`、`LOC`、`BOK`、`VER`、`TIM`、`OFF`、`ORG`、`KIN`、`AST` 等英文缩写可以继续使用 `var(--font-sans)` 小号无衬线。

### 3.3 具体字号/字重参考

以下来自 `FeaturedLetterViewer` 的字体体系，分析页面应对齐：

| 元素 | 字体 | 字重 | 字号 | 字间距 |
|------|------|------|------|--------|
| 页面主标题 | `var(--font-serif)` | 600 | ~30-34px | `.05-.06em` |
| 模块标题 | `var(--font-serif)` | 500-600 | ~16-24px | `.04-.05em` |
| 正文/选项文字 | `var(--font-serif)` | 400 | ~12-14px | normal |
| 小标签（英文） | `var(--font-sans)` | 700-800 | 8-9px | `.14-.2em` |
| 提示/辅助文字 | `var(--font-serif)` | 400 | 10-11px | normal |
| 按钮文字 | `var(--font-serif)` | 400-500 | 11-12px | normal |

### 3.4 禁止事项

- ❌ 不允许主标题用宋体、选项用无衬线同时出现
- ❌ 不允许只修一两个标题就完事
- ❌ 不允许引入 Google Fonts 或新外部字体

---

## 四、修改"预设模板"按钮

`TemplatePicker.tsx` 中的按钮：

1. 紫色背景 `bg-[var(--purple)]`，**白色文字 `text-white`**
2. 下拉箭头也必须是白色
3. hover/focus/active 状态下文字和箭头不能变深色
4. hover 时背景可稍微加深 `hover:bg-[var(--purple-deep)]`

"重置"按钮保持次级样式：白色背景、深色文字、浅灰边框。

---

## 五、删除重复的配置说明区域

当前 `DimensionPanel.tsx` 的 header（约第 212-223 行）：

```tsx
<header className="border-b border-[var(--line)] px-5 py-4 sm:px-6">
  <p>自选分析配置</p>
  <h2>勾选想要观察的维度</h2>
  <p>先选择维度，再设置行列；只有点击"开始分析"后才会更新结果。</p>
</header>
```

**彻底删除整个 header 区块**，包括其中的文字、卡片外框、背景、下方分割线和多余空白。

---

## 六、删除独立的"人物—收信人"区域

当前 `dimensions.ts` 中"人物"是一个独立 group，里面只有 `recipient`（收信人）一个 item：

```typescript
{
  category: "人物",
  items: [{ key: "recipient", label: "收信人" }],
},
```

**删除这个独立 group**。将 `recipient`（收信人）**移到"时间"group 中**，放在年份、时期旁边：

```typescript
{
  category: "时间",
  items: [
    { key: "year", label: "年份" },
    { key: "period", label: "时期（晚清 / 民初）" },
    { key: "recipient", label: "收信人" },
  ],
},
```

"收信人"不再作为 PER 实体子类型的附属，而是一个与年份、时期并列的独立维度。页面上也不再有独立的"人物—收信人"区域。

---

## 七、合并"事件类型"和"行为类型"

当前是分成两个独立的大区域（两个 group），上下排列，中间有分割线。

**合并为一个模块**，模块标题为 **"事件与行为"**。

桌面端布局（同一行）：

```
事件类型：[文献活动] [学术活动] [社会交往] [政治时局] [家族事务]
行为类型：[请求] [展示] [告知] [赞扬] [维系] [训导] [协商]
```

要求：
1. 两个类型在一行或紧凑的纵向排列
2. 两组之间保留合理横向间隔
3. 不要用两条贯穿的横线把两组拆开
4. 每组选项内部紧凑横向排列，自动换行
5. 窄屏允许上下两行
6. 保留现有的多选功能和数据字段（事件类型和行为类型仍是独立的数据字段）

---

## 八、实体大类选择（第一层）

### 8.1 不再使用九宫格大卡片

删除当前"实体类型"区域的 3 列 grid 大卡片布局。替换为**紧凑、横向自适应宽度的多选标签/chip**。

### 8.2 模块标题："选择分析维度"

九个实体大类横向排列，自动换行：

```
选择分析维度

[人物 PER] [地点 LOC] [书籍 BOK] [版本 VER] [时间 TIM]
[官职 OFF] [机构 ORG] [亲属 KIN] [星命 AST]
```

标签视觉：

- **未选**：白色/接近背景色 + 浅灰边框 `border-[var(--line)]` + 深色文字
- **已选**：浅紫色背景 `bg-[var(--purple-pale)]` + 紫色边框 `border-[var(--purple)]` + 紫色/深色文字 + ✓ 勾选标记
- **不需要**等宽布局、固定三列网格、单独展开箭头
- 标签宽度自适应文字
- 点击整个标签区域即可选中/取消

英文缩写（PER、LOC 等）使用 `var(--font-sans)` 小号无衬线，中文标签使用 `font-[var(--font-serif)]`。

---

## 九、二级分类（第二层）：细分范围

### 9.1 模块标题："细分范围"

标题旁边显示轻量提示（小号辅助文字，不要大说明卡片）：

> 可选；未选择细分项时，默认包含该维度下的全部内容。

### 9.2 显示逻辑

- **只显示用户已选中的实体大类**对应的二级分类
- 没选任何实体大类 → 不显示二级分类，可显示轻量提示"请先选择至少一个分析维度"
- 选了"人物" → 显示人物二级分类
- 选了"人物+地点+书籍" → 显示这三类的二级分类

### 9.3 每个大类的二级分类

所有标签从 `getSecondaryCategories(code)` 动态读取。以下是各分类的完整标签（以实际代码输出为准，**标签文字使用 `font-[var(--font-serif)]`**）：

**人物（PER）**：
`[全部人物] [叶德辉本人] [收信人] [同时代人] [叶氏家族] [日本人士] [历史人物] [待考]`

**地点（LOC）**：
`[全部地点] [国家] [省／省级] [府／州／县级] [城市] [街道／具体地点] [地区／模糊区域] [山岳] [水体] [设施／建筑]`

**书籍（BOK）**：
`[全部书籍] [经部] [史部] [子部] [集部] [今人著作] [工具书] [先祖著作]`

**版本（VER）**：
`[全部版本] [宋本／宋刻] [元本／元刻] [明本／明刻] [清本／清刻] [刻本／刊本] [抄本／稿本／写本] [活字本／排印本] [拓本／碑帖] [影印本／石印本] [版本状态] [和刻本]`

**时间（TIM）**：
`[全部时间] [相对时间] [绝对时间] [时段／频率] [历史时期]`

**官职（OFF）**：
`[全部官职] [中央文官] [地方官] [武职] [学官／文教] [民国新职] [外交／涉外]`

**机构（ORG）**：
`[全部机构] [出版／书店] [图书馆／藏书机构] [学校／书院] [政府／衙门] [政党／派系] [公司／银行] [书斋／堂号]`

**亲属（KIN）**：
`[全部亲属] [祖先／先世] [子孙／后嗣] [父系／宗族] [姻亲／婚姻] [家族合称]`

**星命（AST）**：
`[全部星命] [五行／干支] [星宿／星次] [命理／八字] [易学／占卜] [杂占／风水]`

如果某个大类暂时没有二级分类数据，显示一行轻量提示："该维度暂未设置细分项，将按全部内容分析。"

### 9.4 二级分类标签视觉

紧凑多选标签/chip：

- **未选**：白色背景 + 浅灰边框 + 深色文字
- **已选**：浅紫背景 `bg-[var(--purple-pale)]` + 紫色边框 `border-[var(--purple)]` + ✓ 标记
- 横向排列，自动换行
- 标签宽度适应文字
- 每组之间有适当纵向间距，用轻分隔线区分
- **不要**用厚重边框大卡片包裹每个分组
- **不要**使用固定高度或等宽布局

---

## 十、二级分类默认逻辑

1. **选了实体大类但没选二级分类** → 默认分析该大类全部内容
2. **选了具体二级分类** → 只分析选中的二级分类
3. **"全部XX"是快捷选择项**：
   - 选"全部"→ 取消该大类下其他具体二级分类，按全部内容分析
   - 已选"全部"+ 再选具体二级分类 → 自动取消"全部"
4. **取消实体大类** → 隐藏对应二级分类。推荐在当前页面会话内暂存二级选择（用户重新勾选大类时可恢复），"重置"后彻底清空
5. **不允许**：大类已取消但隐藏的二级分类仍参与分析、不选二级就无法分析、必须逐个勾选才能表示"全部"

---

## 十一、预设模板联动

保留现有 6 个预设模板（`lib/analysis/dimensions.ts` 中的 `TEMPLATES`）。选择模板后同步更新：

- 实体大类勾选
- 二级分类勾选（如模板只配了大类，按"全部"处理）
- 事件类型勾选
- 行为类型勾选
- 行列配置和分析参数

下拉菜单文字使用 `font-[var(--font-serif)]`，当前选中模板有明确状态。

---

## 十二、重置逻辑

点击"重置"后恢复初始状态：

- 清空实体大类勾选
- 清空二级分类
- 清空事件/行为类型勾选
- 清空行列分析配置
- 清除预设模板状态
- 清除分析结果，恢复空状态
- 恢复按钮可用状态

---

## 十三、页面推荐结构（从上到下）

1. **顶部导航栏**（SiteHeader）
2. **页面标题区域**：
   - 小标题："书信标注数据探索"
   - 主标题："自选维度分析"
   - 副标题："交叉观察书信中的实体、事件与行为"
   - 右侧：`[预设模板▼]` `[重置]`
3. **精选分析视图区域**（默认展示区，先占位，后续替换）
4. **分隔提示**："想自己探索？勾选维度开始分析"
5. **选择分析维度**（九个实体大类紧凑标签）
6. **细分范围**（仅已选大类的二级分类，可选）
7. **事件与行为**（桌面端同一行）
8. **行列配置**（保留现有行列调整 + 统计指标 + 图表类型选择）
9. **筛选条件**（可选，保留现有 FilterRow）
10. **已选条件摘要**（轻量展示，不要太占高度）
11. **"开始分析"主按钮**
12. **分析结果**（透视表 + 图表，左右排列）

---

## 十四、间距与视觉

1. 页面左右边距沿用 `site-container`（`width: min(1420px, calc(100% - 72px))`）
2. 模块上下间距规律，删除冗余空白
3. 不使用九个等高矩形卡片
4. 不使用过强的大阴影
5. 紫色只作强调色，不大面积铺满
6. 边框颜色克制（`var(--line)` / `var(--line-dark)`）
7. 风格接近数字人文研究页面，不是 SaaS 后台

---

## 十五、响应式

- 桌面端：实体大类横向排列自动换行；事件和行为尽量放一行；二级分类横向排列
- 平板端：实体大类多行；事件和行为可各占一行
- 移动端：所有标签自然换行，不允许横向溢出或页面左右滚动

---

## 十六、可访问性

1. 所有可点击标签有 hover / focus / active 状态
2. 支持键盘 Tab 导航，Enter/Space 切换选中
3. 选中状态除颜色外还需有勾选图标 + 边框变化
4. 下拉菜单有正确的 aria 属性
5. 点击范围覆盖整个标签

---

## 十七、文件修改清单

### 需重写

| 文件 | 说明 |
|------|------|
| `lib/analysis/dimensions.ts` | 维度树：收信人移到"时间"组；事件和行为保持独立字段但视觉合并 |
| `components/analysis/DimensionPanel.tsx` | 重写：删除 header 说明区域；实体大类改为 chip 标签式；二级分类渐进展示；合并事件+行为模块 |
| `components/analysis/AnalysisPage.tsx` | 重写布局和字体 |
| `components/analysis/TemplatePicker.tsx` | 按钮紫色底白字；字体统一 |

### 需微调

| 文件 | 说明 |
|------|------|
| `components/analysis/ChartPanel.tsx` | 导入路径确认 `esm/core`；字体统一 |
| `components/analysis/PivotTable.tsx` | 字体统一 |
| `components/analysis/FilterRow.tsx` | 字体统一 |
| `app/globals.css` | 细滚动条（5px） |

### 不需改动

| 文件 | 原因 |
|------|------|
| `lib/analysis/aggregator.ts` | 逻辑正确，不变 |
| `lib/analysis/echarts-builder.ts` | 逻辑正确，不变 |
| `lib/analysis/types.ts` | 类型定义已适配 |
| `lib/analysis/dimension-values.ts` | 逻辑正确，不变 |
| `app/analysis/page.tsx` | 入口不变 |

---

## 十八、边界情况

- [ ] 未勾选维度 → "开始分析"灰显
- [ ] 76 封无年份 → "未知" + 排除开关
- [ ] 勾父级 → 子级展开；取消父级 → 子级清空（但页面会话内暂存）
- [ ] 实体子类型从 `getSecondaryCategories()` 动态生成
- [ ] 分析结果在页面下方，上方默认展示区不受影响
- [ ] 滚动条全局 5px
- [ ] **所有文字（除英文缩写）使用 `font-[var(--font-serif)]`**
- [ ] 预设模板按钮白字
- [ ] 导出 CSV UTF-8 BOM
- [ ] 图表不兼容时灰显 + 原因
- [ ] ECharts 导入路径 `echarts-for-react/esm/core`

---

## 十九、禁止事项

- ❌ 保留九宫格大卡片
- ❌ 折叠后留大块空白
- ❌ "收信人"在两个地方重复出现
- ❌ 事件和行为作为两个距离很远的大模块
- ❌ "预设模板"按钮深色文字
- ❌ 只修主标题字体、其他区域不统一
- ❌ 删除二级分类
- ❌ 未选二级分类时无法分析
- ❌ 页面加载时同时展示所有二级分类
- ❌ 破坏预设模板或分析功能
- ❌ 添加大量渐变、阴影、圆角胶囊
- ❌ 修改无关页面
